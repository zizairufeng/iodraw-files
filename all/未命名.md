```mermaid
digraph G {
    rankdir=LR;
    node [shape=box];

    上传训练集 -> 数据预处理;
    数据预处理 -> 数据清洗;
    数据预处理 -> 数据标注;
    数据预处理 -> 特征提取;
    数据清洗 -> 模型训练;
    数据标注 -> 模型训练;
    特征提取 -> 模型训练;
    模型训练 -> 选择算法;
    选择算法 -> 训练模型;
    训练模型 -> 保存模型;
    保存模型 -> 模型优化;
    模型优化 -> 调整超参数;
    模型优化 -> 交叉验证;
    模型优化 -> 评估模型性能;
    评估模型性能 -> 模型下载;
}
```